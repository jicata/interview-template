{
  "info": {
    "name": "Interview Template API",
    "description": "GENERATED from the FastAPI OpenAPI schema by `backend/scripts/gen_postman.py`. Do not hand-edit \u2014 regenerate. This is the repo-owned executable wire contract; a route change without a matching delta here is a review finding.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8000",
      "type": "string"
    }
  ],
  "item": [
    {
      "name": "GET /hello",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/hello?name=World",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "hello"
          ],
          "query": [
            {
              "key": "name",
              "value": "World",
              "description": "",
              "disabled": false
            }
          ]
        },
        "description": "Hello"
      },
      "response": []
    },
    {
      "name": "POST /orders",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": {
          "raw": "{{baseUrl}}/orders",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "orders"
          ]
        },
        "description": "Start a new empty draft order for a customer.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"customer_id\": 0\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "response": []
    },
    {
      "name": "GET /orders/{order_id}",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/orders/{order_id}",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "orders",
            "{order_id}"
          ]
        },
        "description": "Read an order back with every line priced and the order total."
      },
      "response": []
    },
    {
      "name": "POST /orders/{order_id}/lines",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": {
          "raw": "{{baseUrl}}/orders/{order_id}/lines",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "orders",
            "{order_id}",
            "lines"
          ]
        },
        "description": "Add units of a product to a draft order.\n\nRejects a quantity that is not a whole number of packs. Returns the whole\nrecomputed order, so the running total needs no second request.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"product_id\": 0,\n  \"quantity\": 0\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "response": []
    },
    {
      "name": "DELETE /orders/{order_id}/lines/{line_id}",
      "request": {
        "method": "DELETE",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/orders/{order_id}/lines/{line_id}",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "orders",
            "{order_id}",
            "lines",
            "{line_id}"
          ]
        },
        "description": "Remove a line and return the recomputed order."
      },
      "response": []
    },
    {
      "name": "GET /products",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/products",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "products"
          ]
        },
        "description": "The product catalogue, with each product's pack size and current price."
      },
      "response": []
    }
  ]
}
