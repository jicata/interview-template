{
  "info": {
    "name": "Interview Template API",
    "description": "GENERATED from the FastAPI OpenAPI schema by `backend/scripts/gen_postman.py`. Do not hand-edit \u2014 regenerate. This is the repo-owned executable wire contract; a route change without a matching delta here is a review finding.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8000",
      "type": "string"
    }
  ],
  "item": [
    {
      "name": "GET /customers",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/customers",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "customers"
          ]
        },
        "description": "List Customers"
      },
      "response": []
    },
    {
      "name": "POST /customers/{customer_id}/draft/lines",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "url": {
          "raw": "{{baseUrl}}/customers/{customer_id}/draft/lines",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "customers",
            "{customer_id}",
            "draft",
            "lines"
          ]
        },
        "description": "Merge a batch of product/quantity lines onto a customer's draft order.\n\nFinds the customer's draft order or creates one, then writes every line\nin a single transaction \u2014 either the whole batch lands or none of it\ndoes. A product already on the draft has its quantity increased rather\nthan gaining a second row; duplicate product ids within the batch are\nsummed before writing.",
        "body": {
          "mode": "raw",
          "raw": "{\n  \"lines\": [\n    {\n      \"product_id\": 0,\n      \"quantity\": 0\n    }\n  ]\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        }
      },
      "response": []
    },
    {
      "name": "GET /customers/{customer_id}/reorder-suggestions",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/customers/{customer_id}/reorder-suggestions?",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "customers",
            "{customer_id}",
            "reorder-suggestions"
          ],
          "query": [
            {
              "key": "as_of",
              "value": "",
              "description": "",
              "disabled": true
            }
          ]
        },
        "description": "List products this customer is due to reorder, most overdue first.\n\n`as_of` is the reference date the cadence is projected against; it\ndefaults to today when omitted. A product due exactly on `as_of` is\nincluded \u2014 `days_overdue == 0` counts as due, not merely approaching."
      },
      "response": []
    },
    {
      "name": "GET /hello",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{baseUrl}}/hello?name=World",
          "host": [
            "{{baseUrl}}"
          ],
          "path": [
            "hello"
          ],
          "query": [
            {
              "key": "name",
              "value": "World",
              "description": "",
              "disabled": false
            }
          ]
        },
        "description": "Hello"
      },
      "response": []
    }
  ]
}
